# Base URL of the sih26188 FastAPI backend (see backend/main.py).
# The reference backend runs on port 8000 by default (uvicorn main:app --reload --port 8000).
VITE_API_URL=https://docverify-pjff.onrender.com/
