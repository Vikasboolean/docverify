# Comma-separated origins permitted to call this API in development.
# Exact frontend origin(s), without a trailing slash. Add local development
# too when it is needed: http://localhost:5173
ALLOWED_ORIGINS=https://docverify-seven.vercel.app
# Change this to a long, random value before exposing the service publicly.
VERIGATE_SECRET=OPENCLOSE
